// import { RequestAPI, ResponseAPI } from "@/typescript/express";

// export const exportDatabase = (req: RequestAPI, res: ResponseAPI) => {};

export const importDatabase = (/*req: RequestAPI, res: ResponseAPI */) => {
  // data
};

// TODO: create database controllers
